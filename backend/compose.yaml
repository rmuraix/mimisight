services:
  app:
    build:
      context: .
      dockerfile: dev.Dockerfile
    ports:
    - "8000:8000"
    develop:
      watch:
        - action: sync
          path: .
          target: /app
          ignore:
            - .venv/
        - action: rebuild
          path: ./pyproject.toml